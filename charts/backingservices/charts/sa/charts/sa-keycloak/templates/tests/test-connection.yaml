apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sa-keycloak.fullname" . }}-test-connection"
  labels:
    {{- include "sa-keycloak.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sa-keycloak.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
