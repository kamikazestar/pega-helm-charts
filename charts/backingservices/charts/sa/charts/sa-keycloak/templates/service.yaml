apiVersion: v1
kind: Service
metadata:
  name: {{ include "sa-keycloak.fullname" . }}
  labels:
    {{- include "sa-keycloak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sa-keycloak.selectorLabels" . | nindent 4 }}
